<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width">
	<link rel="stylesheet" href="{$basePath}/css/bootstrap.min.css">
	<link rel="stylesheet" href="{$basePath}/css/extension.css">
	<title>{ifset title}{include title|stripHtml} | {/ifset}Nette Web</title>
</head>

<body>

<div n:foreach="$flashes as $flash" class="alert alert-{$flash->type}">
	{if $flash->type == 'danger'}
    <strong>Chyba!</strong>
	{/if}
    {if $flash->type == 'success'}
		<strong>Hotovo!</strong>
    {/if}
	{$flash->message}
</div>
<div class="container">
	<div class="jumbotron main">
		<div class="jumbotron">
			<h1 class="display-3">Nette test</h1>
			<div class="btn-group">
				<a class="btn btn-lg btn-success" href="/registration" role="button">Registrovat</a>
				<a class="btn btn-lg btn-success" href="/login" role="button">Přihlásit</a>
				<a class="btn btn-lg btn-success" href="/article" role="button">Přidat článek</a>
				{ifset $username}
				<a class="btn btn-lg btn-danger" href="/logout" role="button">{$username} odhlásit <span class="glyphicon glyphicon-menu-right"></span> </a>
				{/ifset}
			</div>
		</div>
	{include content}
		<footer class="footer">
			<span class="label label-info label-lg">&copy; Tomáš Ballák 2017</span>
		</footer>
	</div>
</div>
</script>
</body>
</html>
